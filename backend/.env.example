# Server
NODE_ENV=development
PORT=4000
API_BASE_URL=http://localhost:4000

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# MongoDB Atlas (free tier)
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/free-api?retryWrites=true&w=majority

# Upstash Redis (free tier) - for rate limiting
UPSTASH_REDIS_REST_URL=https://xxx.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-token

# CORS - comma-separated origins (frontend URL)
CORS_ORIGINS=http://localhost:3000

# Firebase Admin (for verifying Firebase Auth tokens). Get from Firebase Console > Project Settings > Service accounts > Generate new key.
# FIREBASE_PROJECT_ID=free-backed
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@free-backed.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
